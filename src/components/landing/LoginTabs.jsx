import { useState } from 'react'; import { Link } from 'react-router-dom';
const roles = [{ label: 'Customer', route: '/customer/login', copy: 'Book a brilliant clean whenever your car needs it.' }, { label: 'Service provider', route: '/provider/login', copy: 'Manage your work and grow with SmartWash.' }, { label: 'Admin', route: '/admin/login', copy: 'Access the SmartWash operations console.' }];
export default function LoginTabs() { const [selected, setSelected] = useState(0); const role = roles[selected]; return <section className="access" id="access"><div className="access-copy"><p className="eyebrow">Your SmartWash</p><h2>Care, on your <em>terms.</em></h2><p>One considered platform for every side of a cleaner, easier car-care experience.</p></div><div className="access-panel"><div className="role-tabs">{roles.map((r, i) => <button className={i === selected ? 'active' : ''} onClick={() => setSelected(i)} key={r.label}>{r.label}</button>)}</div><div className="role-detail" key={role.label}><span className="role-index">0{selected + 1}</span><h3>{role.label}</h3><p>{role.copy}</p><Link to={role.route} className="line-link">Continue to sign in <span>→</span></Link></div></div></section>; }
